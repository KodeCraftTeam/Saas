/**
 * features/tenant/components/TenantBranding.tsx
 *
 * Aplica la marca del tenant (colores, logo) a la interfaz.
 *
 * Props:
 * - tenant: Tenant
 *
 * Características:
 * - Establece variables CSS para los colores del tenant (--primary, --accent)
 * - Renderiza el logo del tenant en el sidebar/header
 * - Se usa dentro de TenantProvider o el layout del dashboard
 */
