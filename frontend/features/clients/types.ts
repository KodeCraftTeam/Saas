/**
 * features/clients/types.ts
 *
 * Tipos de gestión de clientes.
 *
 * Tipos a definir:
 *
 * Client → {
 *   id: string
 *   tenantId: string
 *   firstName: string
 *   lastName: string
 *   email?: string
 *   phone: string
 *   avatar?: string
 *   notes?: string
 *   totalAppointments: number
 *   totalSpent: number
 *   lastVisit?: string
 *   createdAt: string
 * }
 *
 * CreateClientInput → { firstName, lastName, email?, phone, notes? }
 * ClientFilter → { search?: string }
 */
