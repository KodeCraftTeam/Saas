/**
 * features/appointments/constants.ts
 *
 * Constantes relacionadas con citas.
 *
 * Constantes a definir:
 *
 * APPOINTMENT_STATUS_CONFIG → Record<AppointmentStatus, { label: string; color: string; variant: BadgeVariant }>
 *   ej., { scheduled: { label: 'Programada', color: 'blue', variant: 'info' }, ... }
 *
 * DEFAULT_SLOT_DURATION → 30 (minutos)
 * MIN_APPOINTMENT_DURATION → 15 (minutos)
 * MAX_APPOINTMENT_DURATION → 240 (minutos)
 * BOOKING_ADVANCE_DAYS → 30 (cuántos días de anticipación pueden reservar los clientes)
 */
