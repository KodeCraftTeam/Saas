/**
 * shared/components/feedback/LoadingSpinner.tsx
 *
 * Indicador de carga de página completa o en línea.
 *
 * Props:
 * - message?: string → texto debajo del spinner ("Cargando citas...")
 * - fullPage?: boolean → centra en la altura completa de la ventana gráfica
 *
 * Usa Spinner de @shared/components/ui/Spinner
 */
