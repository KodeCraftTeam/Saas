/**
 * shared/components/ui/Table.tsx
 *
 * Tabla de datos con encabezados ordenables y desplazamiento responsivo.
 *
 * Componentes:
 * - Table → contenedor con desplazamiento horizontal en móvil
 * - TableHeader → <thead>
 * - TableBody → <tbody>
 * - TableRow → <tr> con estilo de rayas opcional
 * - TableHead → <th> con indicador de orden opcional
 * - TableCell → <td>
 *
 * Props en TableHead:
 * - sortable: boolean
 * - sortDirection: 'asc' | 'desc' | null
 * - onSort: () => void
 */
