/**
 * shared/components/ui/EmptyState.tsx
 *
 * Marcador de posición para listas vacías con acción opcional.
 *
 * Props:
 * - icon: LucideIcon → ícono grande centrado
 * - title: string → mensaje principal
 * - description: string → mensaje secundario
 * - action?: { label: string; onClick: () => void } → botón de llamada a la acción
 *
 * Usado cuando: no hay citas, no hay clientes, no hay resultados, etc.
 */
