/**
 * shared/components/ui/Avatar.tsx
 *
 * Avatar de usuario con imagen o iniciales de respaldo.
 *
 * Props:
 * - src: string → URL de la imagen
 * - alt: string → texto alternativo de la imagen
 * - fallback: string → texto de iniciales (ej., "JD")
 * - size: 'sm' | 'md' | 'lg' (predeterminado: 'md')
 *
 * Muestra la imagen si se proporciona src, de lo contrario muestra las iniciales de respaldo.
 * Forma circular con color de fondo basado en las iniciales.
 */
