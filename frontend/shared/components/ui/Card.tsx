/**
 * shared/components/ui/Card.tsx
 *
 * Componente contenedor con subcomponentes para contenido estructurado.
 *
 * Componentes:
 * - Card → contenedor con borde, sombra y relleno
 * - CardHeader → sección superior
 * - CardTitle → texto de encabezado
 * - CardDescription → texto de subtítulo
 * - CardContent → área de contenido principal
 * - CardFooter → sección inferior (acciones)
 *
 * Props en Card:
 * - className: string
 * - children: ReactNode
 * - padding: 'sm' | 'md' | 'lg' (predeterminado: 'md')
 */
