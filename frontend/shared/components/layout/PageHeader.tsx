/**
 * shared/components/layout/PageHeader.tsx
 *
 * Encabezado de página con título, descripción, migas de pan y acciones.
 *
 * Props:
 * - title: string
 * - description?: string
 * - actions?: ReactNode → botones a la derecha (ej., "Nueva Cita")
 * - breadcrumbs?: { label: string; href?: string }[]
 *
 * Diseño: migas de pan en la parte superior, luego título + acciones en una fila, descripción debajo.
 */
