/**
 * shared/types/common.ts
 *
 * Tipos de utilidad comunes usados en toda la aplicación.
 *
 * Tipos a definir:
 *
 * ID                → string (UUID)
 * Timestamp         → string (fecha ISO 8601)
 * PaginationParams  → { page?: number; limit?: number }
 * SortParams        → { sortBy?: string; sortOrder?: 'asc' | 'desc' }
 * SearchParams      → { search?: string }
 * FilterParams      → PaginationParams & SortParams & SearchParams
 */
