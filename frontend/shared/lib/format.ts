/**
 * shared/lib/format.ts
 *
 * Utilidades de formato de datos para visualización.
 *
 * Funciones a implementar:
 * - formatPhone(phone: string) → "(555) 123-4567"
 * - formatCurrency(amount: number) → "$25.00"
 * - formatName(first: string, last: string) → "John D."
 * - getInitials(first: string, last: string) → "JD"
 * - truncate(text: string, maxLength: number) → "This is a long..."
 * - slugify(text: string) → "my-barber-shop"
 */
